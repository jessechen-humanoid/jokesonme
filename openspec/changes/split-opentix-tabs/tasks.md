## 1. Nav tab 命名與對應

- [x] 1.1 在 `js/shared.js` 將「票數追蹤」tab 改名為「追蹤演出」，並新增「追蹤售票」tab 指向 `opentix-analytics.html`（navigation tab for OpenTix Tracker）

## 2. Analytics iframe 嵌入頁面

- [x] 2.1 以頁面結構複用方式新建 `opentix-analytics.html`，複製 `opentix.html` 結構，iframe src 指向 `/analytics?embed=true`（analytics iframe embed page）
- [x] 2.2 驗證兩個 tab 皆正常顯示並載入正確的 iframe 頁面
- [ ] 2.3 Commit 並 push 至 GitHub
