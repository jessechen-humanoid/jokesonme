## 1. 備份與資料模型擴充

- [x] 1.1 備份 Google Sheet「收支紀錄」為 CSV 存檔（migration 前置）
- [x] 1.2 在「收支紀錄」sheet 擴增 `paidByFund` 欄位並 backfill 為 false，落實 paidByFund field on transactions
- [x] 1.3 在「收支紀錄」sheet 擴增 `autoGenerated` 欄位並 backfill 為 false，落實 autoGenerated field on transactions
- [x] 1.4 在「演出清單」sheet 新增「共同基金支出」（對應 Pre-loaded default shows 與 Decision: 新增「共同基金支出」作為虛擬 show（B2））

## 2. GAS 後端：稅務預留計算

- [x] 2.1 在 `gas/Code.gs` 新增 Centralised tax rate constant `TAX_RESERVE_RATE = 0.03`，對應 Decision: 稅率採硬編碼常數
- [x] 2.2 實作 Automatic tax reserve expense per show and allocation group 的核心函式 `recalcTaxReserveForShow(showName)`，對應 Decision: 稅務預留採「per show × per excludedMembers 分組」一筆自動支出（A2 方案）
- [x] 2.3 採 Decision: 自動稅支出的重算採「整組刪除後重建」——先刪除該 show 所有 `autoGenerated=true AND category='稅務預留'` 交易，再依當前收入分組重建
- [x] 2.4 在 `addTransaction` / `updateTransaction` / `deleteTransaction` 尾端呼叫 `recalcTaxReserveForShow`，落實 Tax reserve recalculation on transaction writes
- [x] 2.5 實作 Auto-generated tax reserve is read-only 與 Auto-generated transaction write protection：後端 validation 拒絕對 `autoGenerated=true` 的 update/delete（recalc 內部呼叫除外）
- [x] 2.6 決定並套用自動稅支出的 `date`（採該組最新收入 date）與 `recorder`（固定「系統」）

## 3. GAS 後端：基金支付機制

- [x] 3.1 `getAnalytics` 計算時排除 `paidByFund=true` 交易，落實 paidByFund expenses excluded from common expense aggregation 與 Decision: paidByFund 支出不計入共同支出
- [x] 3.2 `getAnalytics` 回傳新增 `fundReserved`（累積提撥）、`fundUsed`（已動用）、`fundBalance`（基金餘額）三個欄位，落實 Common fund balance calculation
- [x] 3.3 儲存新交易時：對 income 強制 `paidByFund=false`；對 `paidByFund=true` 的 expense 強制 `excludedMembers=''`——落實 paidByFund flag on expense transactions 與 Decision: paidByFund 支出的 excludedMembers 強制空
- [x] 3.4 採 Decision: 新增 `paidByFund: boolean` 欄位取代「用 category 判斷」——確認讀寫 API 使用 flag、不依賴 category 字串

## 4. 前端：交易表單與列表

- [x] 4.1 在交易表單（支出模式）新增 "由共同基金支付" checkbox on expense form（只在 expense mode 顯示）
- [x] 4.2 勾選 checkbox 時隱藏 member allocation grid、解除勾選時還原
- [x] 4.3 選擇 show = 「共同基金支出」時自動勾選「由共同基金支付」，落實 "共同基金支出" virtual show defaults paidByFund to true
- [x] 4.4 交易列表為 `autoGenerated=true` 的 row 加上「自動」tag 並停用編輯/刪除按鈕，落實 Auto-generated transaction visual tag in list
- [x] 4.5 在 expense category dropdown 加入「稅務預留」選項，落實 Fixed transaction categories（8 個支出分類）

## 5. 前端：財務分析頁面

- [x] 5.1 共同基金區塊新增「已動用」「基金餘額」兩行，落實更新版 Common fund display
- [x] 5.2 支出分類佔比區塊新增「稅務預留」獨立分類，落實更新版 Expense breakdown by category
- [x] 5.3 驗證 paidByFund expenses excluded from member allocation——確認成員年度分配淨利 / 需匯款金額計算鏈不受 paidByFund 支出影響

## 6. 前端：共用層與 show 清單

- [x] 6.1 在 `js/shared.js` 與其他 show 清單資料源加入「共同基金支出」（對應 Pre-loaded default shows 14 個）
- [x] 6.2 `js/api.js` 支援 `paidByFund`、`autoGenerated` 兩個欄位的讀寫

## 7. 一次性 Migration 與年度會計費

- [x] 7.1 撰寫 One-time migration for existing shows 腳本：掃描所有既有 show × excludedMembers 組合，寫入對應 `autoGenerated=true` 稅務預留支出
- [x] 7.2 Migration 同時寫入 Annual accounting fee recorded from fund（x3,000、show=共同基金支出、category=行政雜支、paidByFund=true、notes=「年度會計費用」），落實 Decision: 年度會計費採「手動建一筆年度交易」而非月度自動
- [x] 7.3 執行 migration 前在測試 sheet 驗證；執行後比對共同淨利、基金餘額、成員年度分配淨利的預期差異

## 8. 驗證與收尾

- [x] 8.1 端對端手動測試：新增/修改/刪除收入、確認自動稅支出同步更新
- [x] 8.2 端對端手動測試：建立 paidByFund 支出、確認共同基金「已動用」與「基金餘額」數字正確、成員分配不受影響
- [x] 8.3 驗證 checklist 頁面是否需要隱藏「共同基金支出」虛擬 show（open question from design）——依實際使用情境決定
- [x] 8.4 更新相關 CLAUDE.md / README（若有）說明新欄位與資料語義
